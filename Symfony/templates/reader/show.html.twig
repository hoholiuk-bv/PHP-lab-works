{% extends 'base.html.twig' %}

{% block title %}Reader{% endblock %}

{% block body %}
    <h1>Reader</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ reader.id }}</td>
            </tr>
            <tr>
                <th>FullName</th>
                <td>{{ reader.fullName }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ reader.email }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_reader_index') }}">back to list</a>

    <a href="{{ path('app_reader_edit', {'id': reader.id}) }}">edit</a>

    {{ include('reader/_delete_form.html.twig') }}
{% endblock %}
