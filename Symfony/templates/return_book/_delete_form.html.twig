<form method="post" action="{{ path('app_return_book_delete', {'id': return_book.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ return_book.id) }}">
    <button class="btn">Delete</button>
</form>
